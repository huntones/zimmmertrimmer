<div dir="rtl">

# אפיון מוצר — פלטפורמת שליחת ועריכת קבצים אונליין

> **סטטוס:** טיוטת אפיון (v1). זהו מסמך מוצר, לא קוד. הוא מדייק ומרחיב את החזון הקיים ומחלק אותו לשלבי ביצוע.
> **הערת מיתוג:** שם-המטרייה של המוצר נקבע ל-**KOLKLI**. "AudioCut" נשאר כשם המודול/הכלי לאודיו בלבד (`app.html`). **הוחלט.**

---

## 0. עקרון-על: איפה כל דבר רץ (ארכיטקטורה היברידית)

זהו העיקרון שמכתיב את כל שאר האפיון, ולכן הוא ראשון.

האתר הקיים רץ **100% בדפדפן, בלי להעלות שום קובץ לשרת** — זו נקודת המכירה של פרטיות ומהירות, ואסור לוותר עליה. מנגד, "שליחת קבצים גדולים" ו"הקבצים שלי" **מחייבים** שרת ואחסון בענן, כי אי אפשר לשלוח קובץ לאדם אחר בלי שהוא יישמר איפשהו.

לכן המוצר **היברידי**, והחלוקה קבועה:

| 🖥️ רץ בדפדפן (מקומי, בלי העלאה) | ☁️ רץ בשרת (מעלה לענן) |
|---|---|
| עריכת אודיו (חיתוך, נרמול, fade, המרה) | שליחת קבצים גדולים + יצירת לינק |
| עריכת תמונות (חיתוך, שינוי גודל, המרת פורמט) | אזור "הקבצים שלי" ואחסון מתמשך |
| חיתוך/המרת וידאו קצר (WebAssembly/FFmpeg בדפדפן) | הגנה בסיסמה, תוקף לינק, התראות הורדה |
| דחיסת תמונה, המרות קלות | המרות כבדות (DOC↔PDF ב-LibreOffice, הסרת רקע AI) |

**כללי אצבע:**
1. כל עוד אפשר לבצע פעולה בדפדפן — עושים אותה בדפדפן (מהיר, פרטי, זול).
2. פעולה עוברת לשרת רק אם היא *מחייבת* שיתוף/אחסון, או שהיא כבדה מדי לדפדפן.
3. בכל מסך שבו קובץ יוצא מהמכשיר — מציגים חיווי ברור ("הקובץ מועלה כדי שנוכל לשלוח אותו"), כדי לא לשבור את הבטחת הפרטיות.

---

## 1. חזון המוצר

פלטפורמת אינטרנט אחת שבה המשתמש מעלה קובץ **פעם אחת** ובוחר מה לעשות איתו: **לשלוח, לערוך, להמיר, לדחוס, לארגן ולהוריד** — הכול מהדפדפן, בלי להתקין תוכנה.

היעד: להפוך לכלי עבודה יומיומי לכל מי שצריך לשלוח קבצים גדולים או לבצע עריכות מהירות, במקום לקפוץ בין עשרה אתרים שונים.

---

## 2. קהלי יעד

עצמאים, עורכי וידאו, צלמים, מעצבים, משרדי פרסום, מוזיקאים, יוצרי תוכן, מורים, סטודנטים, עסקים קטנים ולקוחות פרטיים — כל מי שצריך לשלוח קבצים גדולים או לבצע עריכה/המרה מהירה.

**קהל ליבה ל-MVP:** יוצרי תוכן ומוזיקאים (שכבר מכירים את כלי האודיו הקיים) + כל מי שמחפש תחליף פשוט ל-WeTransfer.

---

## 3. מבנה האתר

### עמוד הבית

מציג את כל הכלים המרכזיים בצורה ברורה, וכן אזור העלאה גדול בראש העמוד:

> "גררו קבצים לכאן או בחרו מהמחשב"

לאחר ההעלאה המשתמש בוחר פעולה: **שליחה / עריכה / המרה / דחיסה / שמירה בענן**.

הכלים המוצגים:
1. שליחת קבצים גדולים ☁️
2. עריכת אודיו 🖥️
3. עריכת תמונות 🖥️
4. עריכת וידאו 🖥️
5. המרת קבצים 🖥️/☁️
6. דחיסת קבצים 🖥️
7. סידור וארגון קבצים 🖥️ *(כבר קיים: `files.html`)*
8. "הקבצים שלי" — למשתמשים רשומים ☁️

---

## 4. מודול שליחת קבצים גדולים ☁️

**מטרה:** לשלוח קבצים גדולים לאנשים אחרים דרך לינק או אימייל.

### תהליך משתמש
1. המשתמש מעלה קבצים.
2. המערכת מציגה רשימת קבצים, גודל וסוג, ואת הנפח הכולל.
3. המשתמש בוחר אופן שליחה: **לינק להעתקה** או **שליחה במייל** לנמען אחד או יותר.
4. אפשרויות: הגנה בסיסמה, תאריך תפוגה, הודעה אישית.
5. המערכת מעלה ומייצרת קישור הורדה ייחודי.
6. הנמען מגיע לעמוד הורדה מסודר (ראו §14).

### פיצ׳רים
* העלאת קבצים גדולים בהעלאה מקוטעת (chunked / resumable) שלא נשברת באמצע.
* Drag & Drop.
* שליחה למספר נמענים.
* הגנה בסיסמה.
* תוקף קישור: יום / 3 ימים / 7 ימים / 30 יום (המקסימום תלוי בחבילה — §11).
* התראה במייל כשהקובץ הורד.
* מחיקה ידנית של הלינק בכל רגע.
* עמוד הורדה ממותג.
* תצוגה מקדימה לתמונות, וידאו, PDF ואודיו.
* הורדה כקבצים בודדים או כ-ZIP.

---

## 5. מודול עריכת אודיו 🖥️

**מטרה:** עריכת אודיו פשוטה ומהירה, כולה בדפדפן. *(כבר קיים בסיס עובד ב-`app.html`.)*

### יכולות
* חיתוך קטע מתוך שיר (שינוי התחלה וסיום).
* Fade In / Fade Out.
* שינוי ווליום ונרמול עוצמה (89/90/91 dB — כבר קיים).
* המרה ל-MP3 / WAV / AAC + בחירת איכות ייצוא.
* חיבור כמה קבצי אודיו לקובץ אחד.
* הסרת שקט מההתחלה והסוף.
* יצירת רינגטון.
* חיתוך אוטומטי ל-15 / 30 / 60 / 90 שניות.

### תהליך
העלאה → הצגת Waveform → בחירת אזור החיתוך → הגדרות סאונד → תצוגה מקדימה → ייצוא והורדה.

---

## 6. מודול עריכת תמונות 🖥️

**מטרה:** עריכת תמונות בסיסית ומהירה, כולה בדפדפן.

### יכולות
* חיתוך, שינוי גודל, סיבוב, היפוך.
* דחיסת תמונה.
* המרות: HEIC→JPG, PNG/JPG/WEBP הדדית.
* שינוי בהירות / קונטרסט / חדות.
* הוספת טקסט, טשטוש אזורים.
* תבניות מידה מוכנות לרשתות: פייסבוק, אינסטגרם, יוטיוב, טיקטוק, סטורי, תמונת פרופיל.
* **הסרת רקע** — ☁️ (דורש מודל AI; לרוב עדיף בשרת, אלא אם רץ מודל קל בדפדפן).

### תהליך
העלאה → פתיחת העורך → עריכה → תצוגה מקדימה → הורדה בפורמט הרצוי.

---

## 7. מודול עריכת וידאו 🖥️ (כבד → ☁️ לפי הצורך)

**מטרה:** עריכת וידאו בסיסית בדפדפן. פעולות קלות (חיתוך, חילוץ אודיו, השתקה) רצות ב-FFmpeg בדפדפן; דחיסה/המרה של קבצים גדולים עשויות לעבור לשרת אם הן איטיות מדי.

### יכולות
* חיתוך התחלה וסיום / קיצור.
* המרות: MOV→MP4, AVI/MKV/WEBM→MP4.
* דחיסת וידאו ושינוי רזולוציה.
* חילוץ אודיו מווידאו / השתקת אודיו.
* Fade In / Fade Out.
* שינוי יחס מסך: 16:9 / 9:16 / 1:1 / 4:5.

### תהליך
העלאה → Timeline → בחירת קטע → בחירת פורמט ייצוא → עיבוד (עם חיווי אחוזים) → הורדה.

---

## 8. מודול המרת קבצים

| המרה | איפה רץ |
|---|---|
| HEIC→JPG, PNG→JPG, WEBP→JPG, JPG→PDF | 🖥️ בדפדפן |
| MOV→MP4, WAV→MP3, MP4→MP3 | 🖥️ בדפדפן |
| PDF→JPG, יצירת ZIP חדש | 🖥️ בדפדפן |
| DOC→PDF, XLSX→PDF, CSV→XLSX | ☁️ בשרת (LibreOffice headless) |

**תהליך:** העלאה → בחירת פורמט יעד → המרה → הורדה.

---

## 9. אזור "הקבצים שלי" ☁️ (משתמשים רשומים)

אזור אישי שמציג: קבצים שהועלו / נשלחו / נערכו, קישורי הורדה פעילים, תאריך תפוגה, כמות הורדות. פעולות: מחיקה ידנית, שכפול שליחה, יצירת תיקיות, סימון מועדפים.

> הערה: סידור הקבצים המקומי (`files.html`) קיים כבר ורץ בדפדפן בלבד. "הקבצים שלי" הוא הגרסה הענן המסונכרנת שלו למשתמשים רשומים.

---

## 10. סוגי משתמשים ותאימות לחבילות

כדי למנוע בלבול — סוגי המשתמשים ממופים ישירות לחבילות התמחור (§11):

| סוג משתמש | חבילה | עיקר |
|---|---|---|
| **אורח** (לא רשום) | — | העלאה עד מגבלה נמוכה, עריכות בסיסיות, קבצים נמחקים אחרי זמן קצר, בלי היסטוריה. |
| **רשום (חינמי)** | Free | היסטוריית קבצים, קישורים שמורים, מגבלה גבוהה יותר, שמירה ארוכה יותר. |
| **פרימיום** | Pro / Business | נפח גדול, עיבוד מהיר, בלי פרסומות, הגנה בסיסמה, מיתוג אישי, נמענים מרובים, כלי עריכה מתקדמים. |

---

## 11. מודל תמחור

| | **Free** | **Pro** | **Business** |
|---|---|---|---|
| נפח לשליחה | עד 2GB | עד 20GB | עד 100GB |
| שמירת קבצים | 3 ימים | 30 יום | לפי הגדרה |
| עריכה | בסיסית | אודיו/תמונות/וידאו מלאה | מלאה |
| הגנה בסיסמה | — | ✔ | ✔ |
| התראות הורדה | — | ✔ | ✔ |
| פרסומות | יש | אין | אין |
| צוות / הרשאות / מיתוג | — | — | ✔ |
| דוחות הורדה | — | — | ✔ |

---

## 12. מסכים מרכזיים

1. עמוד בית · 2. העלאת קבצים · 3. שליחה · 4. הורדה לנמען · 5. עורך אודיו · 6. עורך תמונות · 7. עורך וידאו · 8. המרת קבצים · 9. הקבצים שלי · 10. התחברות/הרשמה · 11. תמחור · 12. שאלות ותשובות · 13. תנאי שימוש ופרטיות · 14. יצירת קשר.

---

## 13. חוויית משתמש

פשטות מעל הכול: העלאה מהירה, כפתורים גדולים וברורים, מעט שלבים, הסבר קצר בכל מסך, חיווי התקדמות באחוזים, הודעות שגיאה ברורות, התאמה מלאה למובייל, תמיכה בעברית ואנגלית, עיצוב נקי ומהיר. *(הבסיס העיצובי — ערכת הצבעים, מצב כהה/בהיר וה-i18n — כבר קיים ב-`files.html` ו-`app.html` וישמש כשפה משותפת לכל המסכים.)*

---

## 14. עמוד הורדה לנמען ☁️

מציג: שם השולח, הודעה אישית, רשימת קבצים + גודל כל קובץ, תצוגה מקדימה (כשאפשר), כפתור הורדה לכל קובץ, הורדת הכל כ-ZIP, תאריך תפוגה, ושדה סיסמה אם הלינק מוגן.

---

## 15. אבטחה ופרטיות

* קישורים ייחודיים לא-ניתנים-לניחוש.
* הגנה בסיסמה אופציונלית.
* הצפנת קבצים באחסון (at rest).
* מחיקה אוטומטית בתום התוקף.
* חסימת סוגי קבצים מסוכנים + סריקה בסיסית (הבסיס לחסימת EXE/DMG כבר קיים ב-`files.html`).
* מחיקה ידנית בכל רגע.
* **הבטחת פרטיות מפורשת:** כל כלי העריכה 🖥️ לא מעלים דבר לשרת; רק שליחה ואחסון ☁️ מעלים — וזה מוצג למשתמש.

---

## 16. תשתית טכנית מומלצת

### צד לקוח 🖥️
React / Next.js · Drag & Drop · עורך אודיו עם Waveform · עורך תמונות בדפדפן · FFmpeg (WebAssembly) לחיתוך/המרות קלות · Timeline לווידאו.

> **הערת מעבר:** האתר הקיים בנוי HTML/CSS/JS סטטי (`index.html`, `app.html`, `files.html`) עם מנועי FFmpeg ו-Essentia מקומיים. אין חובה לעבור ל-React מיד — אפשר להמשיך בסטטי עד ששכבת המשתמשים/השרת מצדיקה מעבר.

### צד שרת ☁️
Node.js / NestJS · תור עבודות (Redis) · FFmpeg (וידאו/אודיו כבד) · Sharp/ImageMagick (תמונות) · LibreOffice headless (DOC/PDF) · PostgreSQL (משתמשים, קבצים, לינקים) · אחסון S3-compatible.

### תורים ועיבוד ☁️
כל פעולה כבדה נכנסת לתור: המרת/דחיסת וידאו, המרת אודיו כבדה, יצירת ZIP גדול, המרת מסמכים, הסרת רקע, עיבוד תמונות מרובות. המשתמש מקבל חיווי התקדמות בזמן אמת.

---

## 17. סטטוסים של קובץ

`ממתין להעלאה` → `בהעלאה` → `הועלה` → `ממתין לעיבוד` → `בעיבוד` → `הסתיים / מוכן להורדה` → (`נמחק` / `פג תוקף`) · וכן `נכשל` בכל שלב.

---

## 18. פיצ׳רים מתקדמים להמשך

העלאה מ-Google Drive / Dropbox · שליחה ל-WhatsApp · חתימה דיגיטלית / איחוד / פיצול PDF · OCR · יצירת ותרגום כתוביות · הסרת רעשי רקע מאודיו · שיפור ו-Upscale לתמונות · API לעסקים · תוסף Chrome.

---

## 19. MVP — גרסה ראשונה

**עיקרון:** לא לבנות הכול. להשען על מה שכבר עובד ולהוסיף את הליבה החסרה — **שליחת קבצים**.

**כבר קיים (לנצל):**
- ✅ עורך אודיו (חיתוך + נרמול + ייצוא) — `app.html`
- ✅ סידור קבצים מקומי (תיקיות, תגיות, הורדת ZIP) — `files.html`
- ✅ עמוד נחיתה דו-לשוני + מצב כהה — `index.html`

**לבנות ל-MVP (לפי סדר):**
1. **שליחת קבצים גדולים** ☁️ — הליבה החדשה: העלאה → לינק → תפוגה → עמוד הורדה לנמען → הורדת ZIP.
2. **המרת MOV→MP4** 🖥️
3. **המרת HEIC→JPG** 🖥️
4. חיבור "הקבצים שלי" ☁️ מעל הסידור הקיים.
5. הרשמה/התחברות + חבילת Free/Pro בסיסית.

*(DOC→PDF וכלי עריכת תמונה/וידאו מתקדמים — לגל שני, אחרי שיש משתמשים.)*

---

## 20. מיצוב שיווקי

> "כל הכלים לקבצים שלך במקום אחד — שליחה, עריכה, המרה והורדה, בלי להתקין שום תוכנה."

או:

> "שלחו קבצים גדולים, ערכו אודיו, תמונות ווידאו — הכול אונליין, פשוט ומהיר. העריכה נשארת אצלכם במחשב."

---

## 21. סיכום

שילוב בין שירות שליחת קבצים גדולים לבין סט כלי עריכה אונליין, תחת הבטחה אחת: **מעלים קובץ פעם אחת ובוחרים מה לעשות איתו**. היתרון — המשתמש לא מתרוצץ בין אתרים, והעריכה עצמה נשארת פרטית בדפדפן. הדרך הנכונה: להתחיל ב-MVP קטן (שליחה + Audio Cutter קיים + MOV→MP4 + HEIC→JPG), למדוד ביקוש, ואז להרחיב בהדרגה.

</div>
